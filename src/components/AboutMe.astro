---
---

<div  id="about-section" class="about-me-container">
  <h2 class="about-me-heading">About Me</h2>
  <p class="about-me-content">
    I'm a full stack web developer, problem solver, and builder of
    responsive web applications. I love learning and implementing new
    technologies, and I'm forever a student of tech. The web is one of the
    most exciting platforms ever created, and I use it to build performant
    and inclusive projects that enrichen and better people's lives. When I'm
    not coding, I enjoy travelling and fixing motorcycles!
  </p>
  <div class="gradient-container"></div>
</div>
<style>
  .about-me-container {
    min-height: 100vh;
    display: grid;
    grid-template: 4em 9em auto 4em/ 25vw 1fr;
    text-align: center;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    position: relative;
  }

  .about-me-heading {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    align-self: flex-start;
    padding-left: 20%;
    grid-column-start: 2;
    grid-row-start: 2;
    width: 100%;
    height: 5em;
    border-radius: 1em 0em 0em 1em;
    border: inset rgb(var(--main-blue));
    border-width: .25em 0 .25em .25em;
    position: relative;
    left: 110%;
    transition: left 0.4s ease-in-out;
    background: var(--background-black);
  }
  
  .about-me-heading::before{
    content: '';
    position: absolute;
    top: -.35em;
    left: -.35em;
    bottom: -.35em;
    right: 0;
    border-radius: 1em 0em 0em 1em;
    background-color: rgb(var(--main-green));
    z-index: -1;
  }
  
  .about-me-content {
    margin: 0 4em 0 4em;
    padding: 4em;
    grid-row-start: 3;
    grid-column: 1/ 3;
    border-style: solid;
    border-width: 0.25em;
    border-color: rgb(var(--main-purple), 0.7);
    border-radius: 1em;
    position: relative;
    top: 100%;
    opacity: 0;
    transition: all 1.5s cubic-bezier(0.6, 0.3, 0.3, 1.01);
    line-height: 1.5em;
    text-indent: 2em;
    font-size: large;
    background: var(--background-black);
    z-index: -1;
  }
  
  .about-me-content::before{
    content: '';
    margin: -1em;
    position: absolute;
    top: .2em;
    bottom: .2em;
    right: .2em;
    left: .2em;
    border-radius: 1em;
    border: 5px solid  rgb(var(--main-green),.7);
  }
  
  .about-me-content::after{
    content: '';
    margin: -1em;
    position: absolute;
    top: -.4em;
    bottom: -.4em;
    right: -.4em;
    left: -.4em;
    border-radius: 1em;
    border: 5px solid rgb(var(--main-blue), .5);
    z-index: -1;
  }

  .gradient-container{
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    background: linear-gradient( 0deg, var(--background-black) 0%, rgba(19, 21, 26, 0) 20%);
  }
    
  .slide-in {
    left: 0%;
  }
  
  .slide-up {
    opacity: 1;
    top: 0%;
  }
</style>
<script>
  const container = document.querySelector(".about-me-container")
  const containerPosition = container.offsetTop
  const aboutHeader = document.querySelector(".about-me-heading");
  const aboutContent = document.querySelector(".about-me-content");

  function animateIn() {
    const midpoint = window.scrollY + (window.innerHeight / 2)
    if (containerPosition < midpoint) {
      aboutHeader.classList.add("slide-in");
      aboutContent.classList.add("slide-up");
    }
  }

  window.addEventListener("onLoad", animateIn);
  window.addEventListener("scroll", animateIn);
</script>
