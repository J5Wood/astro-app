---
import resume from "../assets/resume.pdf"
---

<div class="nav">
  <h1>Jeremy Wood</h1>
  <span class="nav-links">
    <a class="nav-link" href="#about-section">About Me</a>
    <a class="nav-link" href="#project-section">Projects</a>
    <a class="nav-link" href="#contact-section">Contact</a>
    <a class="nav-link" href={resume} target="_blank" download="jeremy/wood/resume">Resume</a>
  </span>
  <div class="animation-container">
    <hr class="animated-1" />
    <hr class="animated-2" />
    <hr class="animated-3" />
  </div>
</div>
<style>
  .nav {
    display: grid;
    grid-template: auto auto / auto 1fr;
    gap: 10px;
    padding-top: 1em;
  }

  .nav h1 {
    display: inline;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 10px;
  }

  .nav-links {
    display: grid;
    grid-template: auto / 1fr 1fr 1fr 1fr;
    align-items: center;
    text-align: center;
  }

  .nav-link{
    text-decoration: none;
    color: white;
  }

  .nav-link:hover {
    cursor: pointer;
    border: .1em solid rgb(0, 236, 181);
    border-radius: 2em;
    transform: translate(-1px,-2px);
  }

  .highlight{
    background-color:  rgb(169, 87, 241, .3);
  }

  .held-button{
    background-color: rgba(0, 255, 251, 0.6);
    transform: translate(6px, 8px);
  }

  hr {
    border-radius: 0 0 0 5px;
    border: none;
    right: 0;
    position: absolute;
  }

  hr.animated-1 {
    height: 1.1em;
    animation: expand-3 2s ease-in-out 0.4s forwards;
    background-color: rgb(0, 236, 181);
  }

  hr.animated-2 {
    height: 0.8em;
    animation: expand-2 2s ease-in-out 0.2s forwards;
    background-color: rgb(169, 87, 241);
  }

  hr.animated-3 {
    height: 0.5em;
    animation: expand-1 2s ease-in-out forwards;
    background-color: rgb(16, 0, 236);
  }

  @keyframes expand-1 {
    0% {
      width: 0%;
    }
    100% {
      width: 87%;
    }
  }

  @keyframes expand-2 {
    0% {
      width: 0%;
    }
    100% {
      width: 84%;
    }
  }

  @keyframes expand-3 {
    0% {
      width: 0%;
    }
    100% {
      width: 81%;
    }
  }
</style>
<script>
  const links = document.querySelectorAll(".nav-link")

  links.forEach(link => {
    link.addEventListener("mouseover",(e) => e.target.classList.add("highlight"))
    link.addEventListener("mousedown",(e) => {
      e.target.classList.remove("highlight");
      e.target.classList.add("held-button");
    })
    link.addEventListener("mouseleave",(e) => {
      e.target.classList.remove("held-button");
      e.target.classList.remove("highlight");
    })
  })

</script>